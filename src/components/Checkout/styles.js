import { makeStyles } from "@material-ui/core/styles";
import { MAX_CONTENT_WIDTH } from "../../constant.js";

export default makeStyles((theme) => ({
  toolbar: { ...theme.mixins.toolbar, marginTop: "5%" },
  root: {},
}));
